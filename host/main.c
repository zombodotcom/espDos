/*
 * host/main.c — placeholder; will load and run the kernel after
 * Tasks 3-7 add the emulator + BIOS plumbing. For now, just a
 * sanity build target.
 */
#include <stdio.h>

int main(void) {
    fprintf(stderr, "[host] dos_host placeholder; build pipeline OK.\n");
    return 0;
}
